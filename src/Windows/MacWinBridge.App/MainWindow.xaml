<Window x:Class="MacWinBridge.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Mac-Win Bridge"
        Width="380" Height="580"
        MinWidth="340" MinHeight="460"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource SurfaceBrush}"
        ResizeMode="CanResizeWithGrip">

    <DockPanel>

        <!-- ── ログパネル (下部にドッキング、デフォルト非表示) ── -->
        <Border x:Name="LogPanel"
                DockPanel.Dock="Bottom"
                Visibility="Collapsed"
                Background="#0A0A18"
                BorderBrush="#333355"
                BorderThickness="0,1,0,0"
                Height="200">
            <DockPanel>
                <!-- ログツールバー -->
                <Border DockPanel.Dock="Top"
                        Background="#11112A"
                        Padding="10,5">
                    <DockPanel>
                        <TextBlock Text="📋 ログ出力"
                                   Foreground="{StaticResource TextBrush}"
                                   FontSize="11" FontWeight="SemiBold"
                                   VerticalAlignment="Center" />
                        <StackPanel DockPanel.Dock="Right" Orientation="Horizontal"
                                    HorizontalAlignment="Right">
                            <Button Content="📁"
                                    Style="{StaticResource IconButton}"
                                    Click="OnOpenLogFolderClick"
                                    Width="28" Height="24"
                                    FontSize="12"
                                    Margin="0,0,6,0"
                                    ToolTip="ログファイルを開く" />
                            <Button Content="🗑️"
                                    Style="{StaticResource IconButton}"
                                    Click="OnClearLogClick"
                                    Width="28" Height="24"
                                    FontSize="12"
                                    ToolTip="ログをクリア" />
                        </StackPanel>
                    </DockPanel>
                </Border>

                <!-- ログ本文 -->
                <ScrollViewer x:Name="LogScroller"
                              VerticalScrollBarVisibility="Auto"
                              HorizontalScrollBarVisibility="Auto"
                              Padding="10,6">
                    <TextBlock x:Name="LogText"
                               FontFamily="Consolas, Courier New"
                               FontSize="11"
                               Foreground="#A0C0A0"
                               TextWrapping="NoWrap" />
                </ScrollViewer>
            </DockPanel>
        </Border>

        <!-- ── メインコンテンツ ── -->
        <ScrollViewer VerticalScrollBarVisibility="Auto">
    <StackPanel Margin="28,24,28,12">

        <!-- ── 接続ステータスバッジ ── -->
        <Border x:Name="StatusBadge"
                Background="#1A1A2E"
                CornerRadius="16"
                Padding="0,20"
                Margin="0,0,0,20">
            <StackPanel HorizontalAlignment="Center">
                <!-- 接続インジケーター -->
                <Grid HorizontalAlignment="Center" Margin="0,0,0,10">
                    <Ellipse x:Name="StatusDot"
                             Width="18" Height="18"
                             Fill="#DC2626" />
                    <!-- 点滅リング (接続中) -->
                    <Ellipse x:Name="StatusRing"
                             Width="28" Height="28"
                             Stroke="#DC2626" StrokeThickness="2"
                             Fill="Transparent"
                             Opacity="0" />
                </Grid>

                <TextBlock x:Name="ConnectionLabel"
                           Text="未接続"
                           FontSize="20" FontWeight="Bold"
                           Foreground="{StaticResource TextBrush}"
                           HorizontalAlignment="Center" />

                <TextBlock x:Name="ConnectionSubLabel"
                           Text="Macが見つかりません"
                           FontSize="12"
                           Foreground="{StaticResource TextMutedBrush}"
                           HorizontalAlignment="Center"
                           Margin="0,4,0,0" />
            </StackPanel>
        </Border>

        <!-- ── 接続操作 ── -->
        <Grid Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <Border Background="#252540" CornerRadius="8" Padding="2">
                <TextBox x:Name="MacHostInput"
                         Text="auto"
                         FontSize="14"
                         Padding="10,7"
                         Background="Transparent"
                         Foreground="{StaticResource TextBrush}"
                         BorderThickness="0"
                         VerticalAlignment="Center" />
            </Border>

            <Button x:Name="ConnectButton"
                    Grid.Column="1"
                    Content="接続"
                    Style="{StaticResource BridgeButton}"
                    Click="OnConnectClick"
                    Width="80"
                    Margin="8,0,0,0" />
        </Grid>

        <!-- ── モニター選択 ── -->
        <Border Style="{StaticResource CardBorder}">
            <StackPanel>
                <Grid Margin="0,0,0,10">
                    <TextBlock Text="🖥️ 切替モニター"
                               FontSize="13" FontWeight="SemiBold"
                               Foreground="{StaticResource TextBrush}"
                               VerticalAlignment="Center" />
                    <Button Content="↻"
                            Style="{StaticResource IconButton}"
                            Click="OnRefreshMonitorsClick"
                            HorizontalAlignment="Right"
                            Width="28" Height="28" FontSize="14"
                            ToolTip="モニター一覧を更新" />
                </Grid>
                <!-- コードビハインドから動的生成 -->
                <StackPanel x:Name="MonitorListPanel" />
            </StackPanel>
        </Border>

        <!-- ── ディスプレイ切替 ── -->
        <Border Style="{StaticResource CardBorder}">
            <StackPanel>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Column="0" Text="🖥️"
                               FontSize="18" VerticalAlignment="Center"
                               Margin="0,0,10,0" />

                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                        <TextBlock Text="ディスプレイ"
                                   FontSize="13" FontWeight="SemiBold"
                                   Foreground="{StaticResource TextBrush}" />
                        <TextBlock x:Name="DisplayModeText"
                                   Text="Windowsモード"
                                   FontSize="11"
                                   Foreground="{StaticResource TextMutedBrush}" />
                    </StackPanel>

                    <Button x:Name="WindowsModeButton"
                            Grid.Column="2"
                            Content="🪟"
                            Style="{StaticResource IconButton}"
                            Click="OnWindowsModeClick"
                            Margin="0,0,6,0"
                            ToolTip="Windowsモード" />

                    <Button x:Name="MacModeButton"
                            Grid.Column="3"
                            Content="🍎"
                            Style="{StaticResource IconButton}"
                            Click="OnMacModeClick"
                            ToolTip="Macモード" />
                </Grid>

                <!-- ── 映像統計 (Macモード時のみ表示) ── -->
                <Border x:Name="VideoStatsPanel"
                        Visibility="Collapsed"
                        Background="#1A1A2E"
                        CornerRadius="8"
                        Padding="12,8"
                        Margin="0,10,0,0">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition />
                            <RowDefinition />
                        </Grid.RowDefinitions>

                        <!-- FPS -->
                        <StackPanel Grid.Row="0" Grid.Column="0" Margin="0,0,0,4">
                            <TextBlock Text="FPS" FontSize="10"
                                       Foreground="{StaticResource TextMutedBrush}" />
                            <TextBlock x:Name="StatsFps" Text="—"
                                       FontSize="16" FontWeight="Bold"
                                       Foreground="{StaticResource TextBrush}" />
                        </StackPanel>

                        <!-- デコード遅延 -->
                        <StackPanel Grid.Row="0" Grid.Column="1" Margin="0,0,0,4">
                            <TextBlock Text="デコード遅延" FontSize="10"
                                       Foreground="{StaticResource TextMutedBrush}" />
                            <TextBlock x:Name="StatsDecodeMs" Text="—"
                                       FontSize="16" FontWeight="Bold"
                                       Foreground="{StaticResource TextBrush}" />
                        </StackPanel>

                        <!-- 解像度 -->
                        <StackPanel Grid.Row="1" Grid.Column="0">
                            <TextBlock Text="解像度" FontSize="10"
                                       Foreground="{StaticResource TextMutedBrush}" />
                            <TextBlock x:Name="StatsResolution" Text="—"
                                       FontSize="14"
                                       Foreground="{StaticResource TextBrush}" />
                        </StackPanel>

                        <!-- 帯域 -->
                        <StackPanel Grid.Row="1" Grid.Column="1">
                            <TextBlock Text="帯域" FontSize="10"
                                       Foreground="{StaticResource TextMutedBrush}" />
                            <TextBlock x:Name="StatsBandwidth" Text="—"
                                       FontSize="14"
                                       Foreground="{StaticResource TextBrush}" />
                        </StackPanel>
                    </Grid>
                </Border>
            </StackPanel>
        </Border>

        <!-- ── 音声ルーティング ── -->
        <Border Style="{StaticResource CardBorder}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" Text="🔊"
                           FontSize="18" VerticalAlignment="Center"
                           Margin="0,0,10,0" />

                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                    <TextBlock Text="音声の出力先"
                               FontSize="13" FontWeight="SemiBold"
                               Foreground="{StaticResource TextBrush}" />
                    <TextBlock x:Name="AudioRoutingText"
                               Text="Win → Mac"
                               FontSize="11"
                               Foreground="{StaticResource TextMutedBrush}" />
                </StackPanel>

                <Button x:Name="AudioToMacButton"
                        Grid.Column="2"
                        Content="🍎"
                        Style="{StaticResource IconButton}"
                        Click="OnAudioToMacClick"
                        Margin="0,0,6,0"
                        ToolTip="MacのスピーカーでWindowsの音を再生" />

                <Button x:Name="AudioToWinButton"
                        Grid.Column="3"
                        Content="🪟"
                        Style="{StaticResource IconButton}"
                        Click="OnAudioToWinClick"
                        Margin="0,0,6,0"
                        ToolTip="WindowsのスピーカーでMacの音を再生" />

                <Button x:Name="AudioToBothButton"
                        Grid.Column="4"
                        Content="🔀"
                        Style="{StaticResource IconButton}"
                        Click="OnAudioToBothClick"
                        ToolTip="両方のデバイスで再生" />
            </Grid>
        </Border>

        <!-- ── Smart KVM ── -->
        <Border Style="{StaticResource CardBorder}">
            <StackPanel>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Column="0" Text="⌨️"
                               FontSize="18" VerticalAlignment="Center"
                               Margin="0,0,10,0" />

                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                        <TextBlock Text="Smart KVM"
                                   FontSize="13" FontWeight="SemiBold"
                                   Foreground="{StaticResource TextBrush}" />
                        <TextBlock x:Name="KvmSubText"
                                   Text="画面端でMac操作に自動切替"
                                   FontSize="11"
                                   Foreground="{StaticResource TextMutedBrush}" />
                    </StackPanel>

                    <TextBlock x:Name="KvmStatusText"
                               Grid.Column="2"
                               Text="停止"
                               FontSize="12"
                               Foreground="{StaticResource TextMutedBrush}"
                               VerticalAlignment="Center" />
                </Grid>

                <!-- KVM エッジ選択 -->
                <Grid Margin="0,10,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <TextBlock Text="Mac 画面の位置:"
                               FontSize="11"
                               Foreground="{StaticResource TextMutedBrush}"
                               VerticalAlignment="Center"
                               Margin="0,0,10,0" />

                    <ComboBox x:Name="MacEdgeCombo"
                              Grid.Column="1"
                              FontSize="12"
                              Background="#252540"
                              Foreground="{StaticResource TextBrush}"
                              BorderThickness="0"
                              Padding="8,5"
                              SelectionChanged="OnMacEdgeChanged">
                        <ComboBoxItem Content="← 左" Tag="Left" />
                        <ComboBoxItem Content="→ 右" Tag="Right" />
                        <ComboBoxItem Content="↑ 上" Tag="Top" />
                        <ComboBoxItem Content="↓ 下" Tag="Bottom" />
                    </ComboBox>
                </Grid>
            </StackPanel>
        </Border>

        <!-- ── USB-C / Thunderbolt 接続情報 ── -->
        <Border Style="{StaticResource CardBorder}">
            <StackPanel>
                <Grid Margin="0,0,0,10">
                    <TextBlock Text="⚡ USB-C 接続速度"
                               FontSize="13" FontWeight="SemiBold"
                               Foreground="{StaticResource TextBrush}"
                               VerticalAlignment="Center" />
                    <Button Content="↻"
                            Style="{StaticResource IconButton}"
                            Click="OnRefreshUsbClick"
                            HorizontalAlignment="Right"
                            Width="28" Height="28" FontSize="14"
                            ToolTip="USB情報を再スキャン" />
                </Grid>
                <StackPanel x:Name="UsbInfoPanel">
                    <TextBlock Text="スキャン中..."
                               FontSize="11"
                               Foreground="{StaticResource TextMutedBrush}" />
                </StackPanel>
            </StackPanel>
        </Border>

        <!-- ── フッター ── -->
        <Grid Margin="0,8,0,0">
            <TextBlock x:Name="FooterText"
                       Text=""
                       FontSize="11"
                       Foreground="{StaticResource TextMutedBrush}"
                       HorizontalAlignment="Left"
                       VerticalAlignment="Center" />

            <Button x:Name="LogToggleButton"
                    Content="📋 ログ"
                    Style="{StaticResource BridgeButton}"
                    Click="OnLogToggle"
                    HorizontalAlignment="Right"
                    Padding="10,4"
                    FontSize="11" />
        </Grid>

    </StackPanel>
        </ScrollViewer>
    </DockPanel>
</Window>
